# 1CS-Project
guh

for back youll need MySql and python with the packages run pip install -r requirements.txt to run the django files
for front youll need npm and run npm install to get the packages needed to run react 